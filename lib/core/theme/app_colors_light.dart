import 'package:flutter/material.dart';

class AppColorsLight {
  // Base
  static const bg = Color(0xFFF6F7FB);
  static const surface = Color(0xFFFFFFFF);
  static const surface2 = Color(0xFFF0F2F8);

  // Text
  static const text = Color(0xFF0B1020);
  static const textMuted = Color(0xFF5D6B8A);

  // Primary (keep same brand)
  static const primary = Color(0xFF6D4CFF);
  static const primarySoft = Color(0xFFE8E3FF);

  // Borders / dividers
  static const border = Color(0xFFE1E6F2);

  // Status
  static const danger = Color(0xFFFF3B4D);
  static const warning = Color(0xFFFFA000);
  static const success = Color(0xFF16A34A);

  // Chips
  static const chip = Color(0xFFEFF2FA);
}
