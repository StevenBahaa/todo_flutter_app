import 'package:flutter/material.dart';

class AppColors {
  // Base
  static const bg = Color(0xFF0B1020); // خلفية غامقة بنفسجي/كحلي
  static const surface = Color(0xFF121A2B); // cards / sheets
  static const surface2 = Color(0xFF182235); // عناصر داخلية

  // Text
  static const text = Color(0xFFEAF0FF);
  static const textMuted = Color(0xFF9AA7C7);

  // Primary (Purple/Indigo)
  static const primary = Color(0xFF6D4CFF); // زر + و Done
  static const primarySoft = Color(0xFF2A1F63);

  // Borders / dividers
  static const border = Color(0xFF23314D);

  // Status
  static const danger = Color(0xFFFF4D5E); // overdue
  static const warning = Color(0xFFFFB020); // medium
  static const success = Color(0xFF22C55E); // low

  // Chips
  static const chip = Color(0xFF1A2640);
}
